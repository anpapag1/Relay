# Relay Backlog

## Local storage / session

- [x] **Scope the localStorage backup to the source** — done via per-site profiles: one record per old-site domain (`relay_site_v1_<domain>`), auto-loaded silently and auto-saved (debounced 500 ms). Loading a different import no longer resurrects the previous import's stale mappings/tables.

## Editing / safety

- [x] **`beforeunload` "unsaved changes" warning** when the drawer/session has dirty edits. Done: `useUnsavedChangesWarning` (mounted in `App.tsx`) arms a `beforeunload` handler while the session holds manual article work (`hasSessionUnsavedWork` selector — excludes/includes, review flags, saved edits) or the drawer has an unsaved draft (reported via the `unsavedChanges.ts` pub/sub bridge). Auto-exclusions recompute on load and don't trigger it.
- [x] ~~**Batch article actions** — exclude/include selected, mark all reviewed, reset auto-exclusions.~~ Skipped by decision — selection checkboxes + a batch toolbar would add UI clutter for a workflow the per-row Exclude/Include/Flag buttons already cover; value unclear.
- [x] **Keyboard shortcuts** — save, toggle include, toggle reviewed, next/prev article, etc. Done: arrow-key prev/next already existed (drawer guard); added `Ctrl/Cmd+S` save (when dirty), `I` toggle include, `R` toggle review, `Escape` close via `useDrawerShortcuts`, skipping editable fields so typing in the HTML editor is untouched.

## Articles / UI

- [x] **Bypass the duplicate-slug constraint** — the reducer auto-excludes articles sharing a slug at load (`reducer.ts` LOAD_SOURCE). The user may manually re-include one, but `generateWxr.ts:81` emits `wp:post_name` verbatim, so two included articles with the same slug would produce an invalid WXR. Done: `generateWxr` now dedupes `wp:post_name` at export — first article keeps the slug, later collisions get WordPress-style `-2`/`-3` suffixes, skipping suffixes already taken by a natural slug.
- [x] **Edit the preview panel in the rendered Gutenberg view** — done: the rendered "After" preview now supports inline block editing — click a paragraph, heading, list item, or quote text to edit it in place; Enter splits the block at the caret (a heading split makes the second half a new paragraph); Backspace at the very start of a block joins it into the previous block or deletes it when empty (removing the whole list/quote once its last item goes); `Ctrl/Cmd+Enter` or `Escape` finishes and commits the edit back into the draft as a manual override. The textarea Edit mode remains for full-source editing.
- [x] **Add edit button on article metadata** — done: an "Edit metadata" button in the metadata panel of the article sidebar turns the title and published-date fields into inline inputs; saving stores `title`/`postDate` overrides used by the build and derived articles. (Scope decision: author stays fixed at `migration` in exports.)
- [x] **Pagination or windowed list** for the Articles tab when the source has thousands of posts. Done: the list virtualizes above a 150-row threshold — only rows near the current scroll position (plus a 10-row overscan buffer) are mounted in a fixed-height scroll container, matching the MappingsTab pattern; below the threshold it renders every row directly.
- [x] **Clear text input button** — add a clear button to the search input in the Articles tab. Done: an inline `×` button appears inside the search field when it has text; clicking it empties the query.
- [x] **Dark mode** theme. Done: every color moved into CSS design tokens (`--relay-*` in `src/theme/index.css`), with a `[data-theme='dark']` palette; a `ThemeProvider`/`useTheme` hook (mounted in `main.tsx`) applies `data-theme` to `<html>`, defaulting to the OS preference and persisting the choice to `localStorage['relay-theme']`; the header button cycles Light → Dark → Auto.
- [x] **onboarding flow with explanatory animations** when using the app for the first time or pressing the help button. This would include a series of modals that guide the user through the main features of the application in a step-by-step manner, with animations to illustrate how to use them. The goal is to help new users understand the functionality and workflow of the app quickly and effectively. Done: a larger, centered, non-blocking floating-card tour (`features/onboarding/OnboardingTour.tsx`) with one step per tab plus a closing Reusability step — each step shows a short description plus a CSS-keyframe illustration: Import loads data via XML export or live scrape feeding the old/new taxonomy tables; Mappings shows old→new matches with checks and an unmatched flag; Settings shows image resizing, text sizing and the button render mode swapping a blue underlined link with a filled button in the same spot in a live preview panel; Articles shows an A4-ratio skeleton preview whose outline auto-cycles Included (green) / Needs review (amber) / Excluded (red) in sync with three status chips beside sidebar-like rows; Build shows checked articles flowing through a spinning converter into a bouncing `export.wxr` download box; Reusability shows taxonomies, mappings and settings saved locally and restored when the site's articles are loaded again. The header tab a step is about gets a soft pulsing glow around its label while its step is open (the last step glows the header's Saved sites button instead), and the tour never changes the active tab — the background stays put. Settings controls and preview animate slowly, out of phase, in short bursts with pauses rather than in lockstep. It auto-opens once on first load (gated by `localStorage['relay_onboarding_seen']`, set on any dismissal) and the header's `?` help button reopens it any time. All steps share one fixed card height; navigation is Back / Next / Done / Skip / × plus arrow keys (Left/Right step, Right on the last step closes) and Escape closes.
- [x] **More helpful error messages** for common failure cases, such as network errors, invalid input, or unexpected server responses. This would involve catching specific error types and displaying user-friendly messages that explain what went wrong and how to fix it. The goal is to improve the user experience by providing clear guidance when issues arise.(eg , "Network error: please check your internet connection","no articles found in the date range","Invalid input provided"). Done: the fetch URL is validated up front (must parse as an `http(s)` URL, else "Enter a valid URL, e.g. https://old-site.example" without calling the network); `probeSite` distinguishes transport failures ("Couldn't reach the server. Check your internet connection and that Relay is running.") from upstream server errors ("The old site returned a server error (HTTP N). Check that it's online and the URL is correct.") from plain not-found ("Couldn't find a WordPress REST API or RSS feed at that address."); and `fetchSite` returns "No articles found in the date range." when the REST/RSS query matches zero posts.
- [x] **Add Reset option on articles** in the Articles tab to allow users to reset the article's metadata and content to its original state. Done: hovering an edited article's status pill reveals a small **Reset** button (`.article-status-cell` hover rule in `src/theme/index.css`). Clicking it dispatches a new `RESET_ARTICLE` action that removes the article's entire override entry — edited content (`editedHtml`), title, published date, exclusion, and review flag — so the row returns to its pristine imported state, status re-derived and no longer counted as unsaved work.
- [x] **Add a copy for debug button** - This would involve adding a "Copy for Debug" button in the article preview UI that, when clicked, copies the article's metadata, content(befor/after), and the visible problem from an input to structured file optimized. The goal is to allow users to easily share article data with developers or support teams for debugging purposes. the copied data will be sent stait to an ai for fixing the issue in the article convertion process. This would help in quickly identifying and resolving issues with article conversion, improving the overall reliability of the application. Done: a "There is a problem with this article" button at the bottom of the article sidebar expands a small form — the user types what looks wrong and hits "Submit for debugging", which downloads a JSON file (`relay-debug-<slug>-<id>.json`) containing the article's metadata, before/after/draft content, the problem description, the active builder, and the conversion settings, then confirms with "Send this file to the dev to patch the problem." (`debugPayload.ts`).
- [x] **Progress bar for live site scraping in import tab** — done: the "Fetch posts" card now shows a progress bar while the site is being scraped, fed by `fetchSite`'s `onProgress` callback (`FetchSiteProgress`). `FetchProgressBar` (a small presentational component) renders a determinate fill with a percentage when a total is known (REST posts — pages × 100 — and media resolution), and an animated indeterminate bar while probing or over RSS where the count is unknown. The bar is replaced by a one-line summary after the import ("Fetched N posts from REST API/RSS feed", plus the truncation warning when applicable), and both clear on "Start over".

## Server

- [x] **Structured request/error logging + `/health` endpoint** — every request logs a JSON line to stdout (`{ts, level, event, method, path, status, durationMs}`); upstream failures add a `proxy_failed` warn with `route`/`target`/`status`/`reason`; unexpected handler throws become `handler_error` errors + JSON 500. `GET /health` returns `{status: 'ok', uptime}` for the container HEALTHCHECK.

## Fix

- [x] **Fix edit metadata button** — done: the metadata panel's "Edit article" button now edits the article's metadata (title + published date) inline instead of switching the preview to Edit mode. `UPDATE_ARTICLE_METADATA` stores the overrides; `runBuild`'s `toExportArticle` and the derived articles both honour them. Scope decision: author editing was left out — the export attributes every post to the fixed `migration` author (see `runBuild.ts`), so editing it would have had no effect on the WXR.
- [x] **Auto taxonomy scraping** — done: `fetchRestTaxonomies` always fetched the *entire* category/tag list from the source site, but `mapToParseResult` kept only terms that appeared on imported articles. It now merges the full fetched lists in (via `taxonomyMaps`), so every category/tag on the source is available for mapping — unused ones carry `count: 0` and still reconcile into the old tables.
- [x] **Metadata edit should also edit New slug, Category, Tags** — done: the "Edit metadata" form in the article sidebar now also edits the new slug (slugified on save), category, and tags. Categories/tags are checkbox lists fed from the destination target tables id'd `category` / `post_tag`, pre-checked from the article's current resolved terms. `UPDATE_ARTICLE_METADATA` stores `newSlug` / `categoryIds` / `tagIds` overrides; the build exports `newSlug` as `wp:post_name` and replaces the mapped terms of that taxonomy with the picked destination terms (`applyTermOverrides` in `core/build/resolveTerms.ts`), and the derived articles show the same values. Clearing a field drops the override and falls back to the parsed/mapped value; `RESET_ARTICLE` wipes them with the rest of the overrides. Scope decision: options come only from target tables id'd `category`/`post_tag` — a table with a different id (e.g. `cats`) offers no dropdown options.